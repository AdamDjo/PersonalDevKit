import { type Config } from 'tailwindcss';

export const borderRadius: Config['theme'] = {
    circle: 'var(--circle)',
    small: 'var(--small)',
    medium: 'var(--medium)',
    'spc-250': 'var(--spacing-250)',
};
