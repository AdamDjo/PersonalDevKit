import { type Config } from 'tailwindcss';

export const borderWidth: Config['theme'] = {
    'ultra-thin': 'var(--border-ultra-thin)',
    thin: 'var(--border-thin)',
    medium: 'var(--border-medium)',
    regular: 'var(--border-regular)',
    strong: 'var(--border-strong)',
};
